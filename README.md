# plotlyshare ... v2?

This is an attempt to host plotlyshare on cloudlfare pages after a rewrite of the frontend in svelte

Backend is still in progress, currently it just displays hardcoded data (all plots in dashboard actually show the same plot)
